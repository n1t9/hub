<div class="px-4 pt-3 py-6">
  <h2 class="text-xl font-bold mt-2 mb-4">
    <% if current_user %>
      <%= t("helpers.home.welcome", name: current_user.name) %>
    <% else %>
      <%= t("helpers.home.welcome_guest") %>
    <% end %>
  </h2>
  <div class="w-full flex flex-col gap-2">
    <div class="px-3 md:px-5 py-2 border border-gray-400 rounded-md bg-white">
      <%= link_to category_path(@categories[0].id) do %>
        <div class="flex items-center space-x-4">
          <div>
            <%= image_tag @categories[0].icon, class: "w-8 h-8" %>
          </div>
          <div>
            <h5 class="text-lg font-bold tracking-tight"><%= @categories[0].name %></h5>
            <p class="text-xs text-gray-700"><%= @categories[0].keywords.map(&:name).join("、") %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="px-3 md:px-5 py-2 border border-gray-400 rounded-md bg-white">
      <%= link_to category_path(@categories[1].id) do %>
        <div class="flex items-center space-x-4">
          <div>
            <%= image_tag @categories[1].icon, class: "w-8 h-8" %>
          </div>
          <div>
            <h5 class="text-lg font-bold tracking-tight"><%= @categories[1].name %></h5>
            <p class="text-xs text-gray-700"><%= @categories[1].keywords.map(&:name).join("、") %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="px-3 md:px-5 py-2 border border-gray-400 rounded-md bg-white">
      <%= link_to category_path(@categories[2].id) do %>
        <div class="flex items-center space-x-4">
          <div>
            <%= image_tag @categories[2].icon, class: "w-8 h-8" %>
          </div>
          <div>
            <h5 class="text-lg font-bold tracking-tight"><%= @categories[2].name %></h5>
            <p class="text-xs text-gray-700"><%= @categories[2].keywords.map(&:name).join("、") %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="px-3 md:px-5 py-2 border border-gray-400 rounded-md bg-white">
      <%= link_to category_path(@categories[3].id) do %>
        <div class="flex items-center space-x-4">
          <div>
            <%= image_tag @categories[3].icon, class: "w-8 h-8" %>
          </div>
          <div>
            <h5 class="text-lg font-bold tracking-tight"><%= @categories[3].name %></h5>
            <p class="text-xs text-gray-700"><%= @categories[3].keywords.map(&:name).join("、") %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="px-3 md:px-5 py-2 border border-gray-400 rounded-md bg-white">
      <%= link_to category_path(@categories[4].id) do %>
        <div class="flex items-center space-x-4">
          <div>
            <%= image_tag @categories[4].icon, class: "w-8 h-8" %>
          </div>
          <div>
            <h5 class="text-lg font-bold tracking-tight"><%= @categories[4].name %></h5>
            <p class="text-xs text-gray-700"><%= @categories[4].keywords.map(&:name).join("、") %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="px-3 md:px-5 py-2 border border-gray-400 rounded-md bg-white">
      <%= link_to category_path(@categories[5].id) do %>
        <div class="flex items-center space-x-4">
          <div>
            <%= image_tag @categories[5].icon, class: "w-8 h-8" %>
          </div>
          <div>
            <h5 class="text-lg font-bold tracking-tight"><%= @categories[5].name %></h5>
            <p class="text-xs text-gray-700"><%= @categories[5].keywords.map(&:name).join("、") %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
<div class="p-4 bg-white">
  <h2 class="text-xl font-bold">
    新着ポスト
  </h2>
  <ul class="mt-4">
    <% @page_posts.each do |post| %>
      <li class="py-2 border-b border-gray-300">
        <%= link_to page_post_path(post), class: "block flex items center" do %>
          <div class="w-2/3">
            <h3 class="font-bold"><%= post.title %></h3>
          </div>
          <div class="ml-1 w-1/3">
            <%= image_tag post.display_cover_image, class: "object-cover" %>
          </div>
        <% end %>
        <div class="mt-1 flex text-sm text-gray-700">
          <% if current_user.page_post_bookmark?(post) %>
            <%= link_to page_post_bookmark_path(post.id), data: { turbo_method: :delete, turbo_confirm: t('flash.unbookmark_confirm') } do %>
              <span class="material-icons-outlined text-base">
                bookmark
              </span>
            <% end %>
          <% else %>
            <%= link_to page_post_bookmark_path(post.id), data: { turbo_method: :post } do %>
              <span class="material-symbols-outlined text-base">
                bookmark
              </span>
            <% end %>
          <% end %>
          <%= link_to page_path(post.page), class: "flex" do %>
            <span class="ml-2">
              <%= image_tag post.page.display_profile_image, class: "w-6 h-6" %>
            </span>
            <span class="ml-1">
              <%= post.page.name %>
            </span>
          <% end %>
          <span class="ml-2">
            <%= time_ago_in_words(post.created_at) %>前
          </span>
        </div>
        <div class="mt-0.5">
          <ul class="flex flex-wrap">
            <% post.page.keywords.each do |keyword| %>
              <li class="mr-1.5">
                <div class="px-1.5 py-0.5 flex items-center rounded border border-gray-400 text-xs leading-5">
                  <%= link_to keyword_path(keyword), class: "text-gray" do %>
                    <%= keyword.name %>
                  <% end %>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
      </li>
    <% end %>
  </ul>
</div>
